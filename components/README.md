# Matchday App

## 🚀 Push to GitHub

```bash
git add .
git commit -m "update"
git push

npm install
npm run dev

git log -1
git branch   - We Want this to be MAIN
git push origin main

git push -u origin main